services:
  mongo:
    image: mongo:7
    container_name: brahmo-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: brahmo
    volumes:
      - brahmo_mongo_data:/data/db
volumes:
  brahmo_mongo_data:
